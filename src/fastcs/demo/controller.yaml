# yaml-language-server: $schema=schema.json
controllers:
  TEMPERATURE:
    controller:
      ip_settings:
        ip: "localhost"
        port: 25565
      num_ramp_controllers: 4
transport:
  - graphql:
      host: localhost
      port: 8083
      log_level: info
  - epicsca: {}
    gui:
      title: Temperature Controller Demo
      output_dir: .
